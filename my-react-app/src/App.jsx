import Table from "./Table"; // Import the Table component

function App() {
  return (
    <div>
      <h1>Flask + MySQL + React</h1>
      <Table /> {/* Display the table from the Flask backend */}
    </div>
  );
}

export default App;